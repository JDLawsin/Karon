import { EmptyState } from "@karon/design-system";

const OwnerTodayStub = () => (
  <EmptyState title="Today's collections">
    Owner totals will land here. Assistants cannot open this page.
  </EmptyState>
);

export default OwnerTodayStub;
